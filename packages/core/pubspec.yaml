name: puzzle_core
description: UI-agnostic core contracts shared by all puzzle modules (module registry, save/settings/audio services, logging).
publish_to: none
resolution: workspace
environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
